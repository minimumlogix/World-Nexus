import { OptimizeParams, OptimizeResult } from "./single.js";
declare const map: {
    optimizeImage(options: OptimizeParams): Promise<OptimizeResult>;
};
export type ImageConverterWorker = typeof map;
export {};
