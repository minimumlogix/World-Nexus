"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createRegistry = exports.getTransferables = exports.isPlainObject = exports.isPlaceholder = exports.FUNCTION_PLACEHOLDER = void 0;
exports.FUNCTION_PLACEHOLDER = "__worker_lib_function__";
const isPlaceholder = (v) => v && typeof v === "object" && exports.FUNCTION_PLACEHOLDER in v;
exports.isPlaceholder = isPlaceholder;
const isPlainObject = (v) => {
    return (v &&
        typeof v === "object" &&
        Object.prototype.toString.call(v) === "[object Object]" &&
        !(v instanceof Uint8Array) &&
        !(v instanceof ArrayBuffer) &&
        !ArrayBuffer.isView(v));
};
exports.isPlainObject = isPlainObject;
const getTransferables = (v, result = []) => {
    if (v instanceof ArrayBuffer) {
        result.push(v);
    }
    else if (ArrayBuffer.isView(v)) {
        result.push(v.buffer);
    }
    else if (Array.isArray(v)) {
        for (const item of v)
            (0, exports.getTransferables)(item, result);
    }
    else if (v && typeof v === "object") {
        for (const key in v)
            (0, exports.getTransferables)(v[key], result);
    }
    return result;
};
exports.getTransferables = getTransferables;
const createRegistry = () => {
    const callbacks = new Map();
    const callbackProxies = new Map();
    const registerCallback = (requestId, fn) => {
        const id = `${requestId}:${Math.random().toString(36).slice(2)}`;
        callbacks.set(id, fn);
        return id;
    };
    const clearCallbacks = (requestId) => {
        for (const key of callbacks.keys()) {
            if (key.startsWith(`${requestId}:`)) {
                callbacks.delete(key);
            }
        }
        for (const key of callbackProxies.keys()) {
            if (key.startsWith(`${requestId}:`)) {
                callbackProxies.delete(key);
            }
        }
    };
    const transformArgs = (requestId, args) => {
        if (typeof args === "function") {
            return { [exports.FUNCTION_PLACEHOLDER]: registerCallback(requestId, args) };
        }
        if (ArrayBuffer.isView(args) || args instanceof ArrayBuffer) {
            return args;
        }
        if (Array.isArray(args)) {
            return args.map((v) => transformArgs(requestId, v));
        }
        if ((0, exports.isPlainObject)(args)) {
            const result = {};
            for (const key in args) {
                result[key] = transformArgs(requestId, args[key]);
            }
            return result;
        }
        return args;
    };
    const resolveArgs = (requestId, args, createProxy) => {
        if ((0, exports.isPlaceholder)(args)) {
            return createProxy(args[exports.FUNCTION_PLACEHOLDER]);
        }
        if (Array.isArray(args)) {
            return args.map((v) => resolveArgs(requestId, v, createProxy));
        }
        if ((0, exports.isPlainObject)(args)) {
            const result = {};
            for (const key in args) {
                result[key] = resolveArgs(requestId, args[key], createProxy);
            }
            return result;
        }
        return args;
    };
    return {
        callbacks,
        callbackProxies,
        registerCallback,
        clearCallbacks,
        transformArgs,
        resolveArgs,
    };
};
exports.createRegistry = createRegistry;
//# sourceMappingURL=common.js.map